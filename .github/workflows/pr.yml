name: PR

on:
  pull_request:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    # Rust CI disabled while Python v2 is the primary implementation.
    if: ${{ false }}
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Build
      run: cargo build --all
    - name: Run tests
      run: cargo test --all -- --show-output
    - name: Check formatting
      run: cargo fmt --all -- --check
