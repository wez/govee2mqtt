# LAN Support (Legacy)

The Python v2 implementation does not include LAN control. LAN-specific logic is part of the legacy Rust implementation in `src/` and is retained for historical reference only.

Engineering note: New work should target the official Platform API v2 and avoid undocumented LAN behavior.
